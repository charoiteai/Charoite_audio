# Настройка диаризации спикеров

*[English](../DIARIZATION.md) · **Русский** · [中文](../zh/DIARIZATION.md)*

Charoite делает два прохода диаризации:

1. **Живой** (во время встречи): модель спикер-эмбеддингов в реальном
   времени помечает чанки как «Собеседник 1/2/…». Нужна эмбеддинг-модель
   ERes2Net в формате ONNX по пути `models/diar/embedding.onnx`
   (выход 512 измерений, вход 16 кГц). Ставится одной командой:

   **Проще всего — кнопкой «Различать голоса собеседников» в мастере первого
   запуска приложения.** Ниже — то же самое из терминала.

   ```bash
   .venv/bin/python scripts/get_models.py --diar          # дефолтная модель
   .venv/bin/python scripts/get_models.py --diar --list   # что ещё есть
   .venv/bin/python scripts/get_models.py --diar --check  # проверить, что стоит
   ```

   Скрипт печатает адрес до скачивания, проверяет, что пришёл именно ONNX и
   что он не обрезан на середине, и кладёт файл туда, где его ищет демон.
   Модели — из [проекта 3D-Speaker](https://github.com/modelscope/3D-Speaker)
   (Apache-2.0; ERes2Net хорошо работает для русского и английского), зеркала
   ONNX собраны для sherpa-onnx. Своя ссылка — `--url`.

   Это единственное место в продукте, кроме опционального облачного слоя,
   которое ходит в сеть: только по запуску руками, один раз, дальше офлайн.
   `--check` не открывает соединений вовсе.
2. **Офлайн-перепроход** (после Стоп): вся запись заново диаризуется
   поканально, эхо между микрофоном и системным звуком отфильтровывается,
   микрофрагменты вливаются в соседние реплики, а имена, прозвучавшие в
   разговоре, расставляет локальная LLM. Результат заменяет живой
   черновик стенограммы.

Без `models/diar/embedding.onnx` Charoite всё равно работает: вместо
меток по голосам используются метки каналов (вы и другая сторона).

Настройка (`config/config.yaml`):

- `live_diarize_threshold` (по умолчанию 0.45) — косинусная близость, при
  которой чанк прикрепляется к уже известному голосу; поднимите порог,
  если разные люди склеиваются в одного, опустите — если один человек
  постоянно распадается на двух.

## Как это измерять

«Путает говорящих» — мнение, пока нет числа. `scripts/diar_bench.py` считает
DER (diarization error rate): долю времени речи, подписанную неверно —
пропущенная речь, речь, услышанная в тишине, и время, отданное не тому
голосу. Метки гипотезы сопоставляются с эталонными, так что «spk0 вместо
Милены» ошибкой не считается: диаризация обязана отличать людей, а не угадывать
имена.

```bash
.venv/bin/python scripts/diar_bench.py --make    # синтетический диалог + разметка
.venv/bin/python scripts/diar_bench.py           # померить оба движка
```

Записей встреч в репозитории нет и быть не может — это чужие разговоры.
Фикстура собирается на месте системным синтезатором речи macOS: четыре разных
голоса произносят реплики, разметка известна точно, потому что мы её и
составили. Это нижняя планка: синтезированные голоса чище живых, перебиваний
и шума комнаты в них нет, поэтому движок, который путает говорящих ЗДЕСЬ, на
живой встрече будет хуже. Обратное неверно — цифры отсюда нельзя выдавать за
качество на реальных встречах.

Замер 30.07.2026 на этой фикстуре (32 с, 4 голоса):

| Движок | DER | Нашёл голосов |
|---|---|---|
| живой режим сейчас (сегментация + эмбеддинг по кускам) | **0.246** | 4 из 4 |
| прежний трекер по чанкам (`--engine live-legacy`) | 0.725 | 1 из 4 |
| проход после встречи (`--engine sherpa`) | 0.296 | 3 из 4 |
| он же, с подсказкой «голосов 4» | 0.248 | 4 из 4 |

Прежний трекер сваливал всех в один голос, и порог на это почти не влиял: от
0.25 до 0.55 результат одинаковый. Причина не в пороге, а в том, что речь
резалась по таймеру (чанки по три секунды), а не по границам реплик: в один
чанк попадает конец одной фразы и начало другой, эмбеддинг получается
смешанный. Сегментация даёт границы, и эмбеддинг считается по куску речи.

Метка на чанк по-прежнему одна — у чанка один текст от распознавания, поэтому
берётся голос, звучавший в нём дольше. Если распознавание тоже пойдёт по
кускам, ошибок станет ещё вдвое меньше: измеренный DER посегментной разметки —
0.090.
