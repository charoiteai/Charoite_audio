# Возможности

*[English](../FEATURES.md) · **Русский** · [中文](../zh/FEATURES.md)*

## Во время встречи

- **Напоминание о записи** — самая дорогая ошибка на встрече это забытая
  кнопка: без звука не будет ни стенограммы, ни минуток, ни узлов графа.
  Когда календарь включён (тот же opt-in тумблер, что и бриф), приложение
  показывает системное уведомление и полосу в окне «встреча „Планёрка“
  началась — начать запись?» с двумя кнопками. Опциональный автозапуск через
  штатные «Объекты входа» macOS позволяет напомнить даже до открытия окна.
  **Запись сама не включается никогда** — только по нажатию;
  «Не сейчас» запоминается, и про эту встречу больше не спрашивают. Окно
  подсказки: от двух минут до начала до десяти минут после — позже предлагать
  поздно, а всплывшая полоса посреди разговора мешает больше, чем помогает.
  Вседневные события и события без других участников встречами не считаются:
  «забрать посылку» в календаре записывать нечего.
- **Живая стенограмма** — реплики склеиваются в абзацы по говорящим, а не
  сыплются строками-чанками. Лёгкая модель дорисовывает границы реплик
  внутри абзаца («— …»), не меняя ни слова (строгая валидация).
- **Живая диаризация** — «Собеседник 1/2/…» по голосам с первых секунд.
  Имя владельца не угадывается никогда; ваш микрофон подписан вашим именем
  из конфига. Если модели голосов нет (`models/diar/embedding.onnx` в
  поставку не входит) или диаризация выключена в конфиге, демон говорит об
  этом статусом на старте встречи: метки пойдут по каналам, и это видно
  сразу, а не после чтения стенограммы.
- **Голос против имени** — имя опознаётся из текста разговора, и лёгкая
  модель иногда присваивает его не тому: басовитого собеседника называют
  «Анной», потому что имя прозвучало рядом. Поверх текстовых проверок стоит
  гейт по голосу: демон считает медианную частоту основного тона метки, а род
  имени спрашивает у лёгкой модели («Саша» и «Женя» — unisex, и они не
  блокируются никогда). Имя отклоняется, только когда обе стороны уверенно
  противоречат друг другу; метка остаётся честным «Собеседник N». Внутри
  андрогинной зоны (≈145-190 Гц, где одной частоты недостаточно) регистр не
  определяется вовсе. Ошибка всегда в пользу молчания: потерянное имя
  дописывается позже, неверное живёт в графе месяцами. Регистр голоса нигде
  не сохраняется и не пишется в документы — он живёт в памяти встречи ровно
  столько же, сколько эмбеддинги (см. [PRIVACY.ru.md](PRIVACY.md)).
- **Живые имена** — как только человек представился (или к нему обратились
  и он ответил), метка «Собеседник N» заменяется на имя — задним числом по
  всей стенограмме и во всех будущих репликах. Модель обязана найти имя в
  тексте — галлюцинации отсекаются. Проверки доверия к имени живут в одном
  месте (`src/speaker_names.py`) и одинаково строги с моделью голосов и без
  неё: имя из `sufler.user_name` не достаётся собеседнику (сравнение по
  словам, поэтому «Игорь» узнаётся в «Игорь Ветров»); имя, прозвучавшее
  только в репликах самого говорящего и без представления, считается
  обращением к другому человеку («Саш, а ты…» не делает говорящего Сашей);
  падежи приводятся к людям графа («Полин» → «Полина»).
- **Молчание модели на разборе имён не выдаётся за успех.** «Имён в разговоре
  не звучало» и «модель не ответила» дают одинаково пустой результат, и
  однажды встреча ушла с метками «Собеседник 1..5», а прогон записался
  полностью удачным. Теперь эти случаи различаются: если модель молчала и
  безымянные метки остались, стенограмма получает строку-предупреждение в
  шапке, а статус встречи — поле `names_pending`. Состояние остаётся `ready`
  (граф обновлён, повторять весь конвейер незачем), но человеку видно, что
  встречу стоит пересобрать, когда модель свободна. В ленте последних встреч
  вместо «Готово» стоит «Готово, имена не определены» — рядом с кнопкой
  «Повторить», которая и есть ответ на эту строку. Пометка исчезает сама:
  повторный прогон переписывает стенограмму целиком.
- **Мгновенный ответ (⚡)** — вопрос собеседника детектится по пунктуации
  STT и стартовым словам; готовый ответ от первого лица приходит за ~2-3 с.
  Вопрос виден в строке статуса, пока ⚡ отвечает; целиком — в _hints.md встречи (в нить идёт чистый ответ — пакет 24.08). Короткие настоящие вопросы проходят
  фильтр: явная вопросная форма — «?» плюс вопросное начало — смягчает
  порог предмета («Что с деплоем?» получает ответ, голое «Что?» — нет).
  Вопрос передаётся модели явно (быстрый триггер слышит его в стриме
  раньше стенограммы), а отказ модели больше не глушит повтор того же
  вопроса до конца встречи.
- **Облачный ответ (☁️, опция)** — параллельно локальному тот же вопрос
  уходит Claude (ваша подписка, `claude` CLI): локальный ответ мгновенный,
  облачный — глубже на 10-20 с позже. По умолчанию выключено — и выключено
  на всех путях: тумблер читает `src/privacy.py`, а проверка стоит внутри
  треда, который запускает `claude`, так что ручной запрос (⌘⇧⏎) мимо неё
  не проходит. При выключенном слое кнопка серая и объясняет почему;
  `SUFLER_NO_CLOUD=1` выключает облако независимо от конфига. Промпт несёт
  только роль пользователя, без списка тем, и требует честности вместо
  уверенности: факты встречи (повестка, цифры, статусы) — из стенограммы,
  нет в ней — ответ так и говорит. Раньше зашитый в промпт список тем в
  начале встречи выдавался моделью как реальная повестка.
- **Автотезисы** — 📌 контрольные точки (решение, срок, поручение) и
  💭 мысли модели по ходу разговора; тяжёлая модель периодически
  пересматривает заметки лёгкой (🔬). 💎 «важное» убран: факты разговора
  ведёт нить — третий поток тех же фактов в ленте был дублированием.
  Перефразы-повторы отсеиваются локальной NLI-моделью (дубль = утверждения
  взаимно следуют друг из друга; уточнение с новым фактом дублем не
  считается). Слой опционален: без модели в `models/nli/` просто выключен —
  см. src/nli.py.
- **Дежавю (⏮)** — если тема разговора совпала с «Ядром» графа (сквозной
  темой прошлых встреч), в тезисы приходит «⏮ уже обсуждалось 15.07,
  статус: …». Совпадение по смыслу (эмбеддинги bge-m3, относительный
  порог), а не по буквам. С 24.08 тезисный слой в приложении выключен
  безусловно (пакет владельца) — дежавю живёт только в headless-запусках
  демона.
- **Авто-бриф в начале встречи (⏮)** — как только по первым репликам ясна
  тема, суфлёр один раз вытаскивает контекст из архива: топ-ядра со
  статусом и датами прошлых обсуждений. Собирается из готовых строк графа,
  без LLM — мгновенно и без выдумок. Конфиг: `meeting_brief`.
- **Живой черновик минуток** — обновляется каждые ~2.5 минуты; финальный
  протокол — по команде или после стопа.
- **Вопрос ассистенту** — спросите текстом посреди встречи: ответ строится
  сначала по живой стенограмме, потом по памяти графа, потом по знаниям
  модели (с пометкой источника).

## После встречи (автоматически)

1. **Пересборка стенограммы** — полная запись диаризуется заново по
   каналам: фильтр эха, слияние микрофрагментов, чистые абзацы, имена.
   Фамилии и аббревиатуры, которые STT пишет по-разному, приводятся к
   канону из графа: словарь — имя узла и его `aliases:` во frontmatter
   (только Люди и капс-узлы систем), основа алиаса заменяется с
   сохранением падежного окончания, а похожие, но не подтверждённые
   слова не трогаются — они уходят в `logs/lexicon_candidates.md` с
   пометкой ✔, когда слова рядом совпадают с полем `отдел:` или телом
   узла: подтвердить новый алиас — одна правка в узле.
2. **Обновление графа** — сущности (Люди/Системы), решения, поручения,
   «Ядра» со статусом и хроникой; заметка встречи со связями.
3. **Архив встречи** — папка «дата — название (2-3 слова)» со всеми
   документами и ссылкой, открывающей граф в Obsidian.
4. **Саммари** — выжимка на минуту чтения: суть одной строкой → темы →
   решили → поручения (кто/что/срок) → открытые вопросы → **связь с
   прошлыми встречами** («было: … (15.07) → сегодня: …») → навигация
   к Минуткам/Разбору/Стенограмме. Объём держит код, а не промпт, и
   жертвует разделами по важности: сначала связь с прошлым, потом
   открытые вопросы, потом обзор тем. Решения и поручения переживают
   обрезку — из-за них выжимку и открывают. Решения к тому же не ищутся
   заново: они уходят в модель готовым блоком из минуток, а если ответ
   всё равно вышел пустым — раздел заполняется из них же кодом.
5. **Разбор** (опция) — вопросы и ответы встречи, задачи, варианты решений
   открытых вопросов, рекомендации к следующей встрече.

**Ревизия ядер** (`src/tier3.py`) — со временем экстрактор расщепляет
сквозную тему на ядра-двойники. Ревизия находит их парами:
эмбеддинг-префильтр (bge-m3) → NLI-судья — и работает сама: инкрементально
после каждой встречи (свежие ядра против всех) и полным прогоном через
`scripts/tier3_cores.py --all-graphs --auto` (можно повесить в cron —
`--auto` сливает только при `tier3_auto_apply: true`, иначе только
помечает; `--apply` остаётся для ручного запуска с глазами).
Полный прогон квадратичен: на трёх сотнях ядер это сорок тысяч пар и часы
работы NLI, поэтому ночью он идёт с `--since-last` — судятся только ядра,
изменившиеся с прошлого раза (отметки по графам в `logs/tier3_last_run.json`),
а всё против всего сверяется раз в неделю, по воскресеньям. Отметка сдвигается
только после состоявшегося прогона: без NLI-модели или с лежащей Ollama ревизия
возвращает пустой результат, и сдвиг вычеркнул бы эти ядра из фокуса навсегда.
Прав два, а не один, потому что цена у правок разная. Обратимые идут
всегда: средняя уверенность даёт пометку «возможный дубль» (её собирает
утренний бриф в раздел «Tier3 просит свести вручную»), вложения («эпизод ⊂
процесс») не сливаются, а связываются взаимными ссылками. Необратимое —
слияние, когда хроника переносится, а от дубля остаётся redirect, —
требует `--apply` в CLI или `sufler.tier3_auto_apply: true` в конфиге. Чем
слабее основание, тем выше планка: ядро от экстрактора не имеет секции
`## Суть`, и судья сравнивает сегодняшние строки статуса, а две живые
задачи одного проекта на них похожи сами по себе. Для такой пары мало
уверенного вердикта — нужна ещё и непересекающаяся хроника: двойники
приходят из разных встреч (в июне тему назвали одним именем, в июле
другим), а два потока одного проекта ведут на одних и тех же. Пара с общей
хроникой помечается, а не сливается. Генерические ядра-«хабы» не трогаются,
перед любой правкой — бэкап в `Ядра/.tier3_backup/`.

**Досье по темам** (`src/dossier.py`, `scripts/nightly_dossier.py`) — сводка
на каждую сквозную тему: состояние, хроника, решённое, открытое, кто в теме
— со ссылками на узлы-источники. Кластер темы — ядро плюс всё, что на него
ссылается; границы задаёт сам человек расстановкой ссылок. Поиск смотрит
в `Досье/_index.json` **первым** и идёт в граф только за деталями, поэтому
на «что в итоге по теме» ответ собирается из одной готовой сводки, а не из
десятка кусков. Пересборка инкрементальная: у досье в frontmatter лежит
отпечаток состава источников (с точностью до микросекунды — правка,
легшая в ту же секунду, что и скан, раньше оставалась незамеченной), совпал
— тема не менялась и модель не вызывается. В очереди ночи несобранные темы
идут ПЕРВЫМИ: сортировка по одному размеру кластера отдавала все слоты
большим темам, а новые маленькие ждали неопределённо долго. Потолок тем —
на прогон, а не на каждый граф. Раз в неделю (в ночь на понедельник) досье
пересобираются целиком: `--full` больше не упирается в этот потолок.
Запись идёт под общим замком графа — тем же, что берут разбор встречи и
облачная ревизия. Ручные дописки живут в разделе «## Правки автора» и переживают
пересборку. Проверить, что найдётся по запросу:
`scripts/nightly_dossier.py --find "текст вопроса"`.

**Ревизия досье облаком — опционально** (`scripts/nightly_dossier_review.py`).
Локальная модель собирает досье массово и дёшево, но не замечает связей:
что решение отменено более поздним, что срок истёк, что два источника
противоречат друг другу. Это видит Opus, и он идёт вторым проходом. При
`sufler.cloud_edit_graph: true` правит сам; по умолчанию **выключено** — тогда
он пишет отчёт `Служебное_ревизия_досье_<дата_время>.md`, а правит человек.
Отчёт пишется в обоих режимах, разделами: применено (+/− строк, число
пометок ⚠️, ссылки до/после, путь к копии), отклонено (с причиной), сбои
шага (сеть, лимит, код возврата — не отказ по содержанию) и
предложено-но-не-применено, когда запись выключена. Ответ облака до записи
проходит проверку, а не «похоже на досье»: ровно пять заголовков в заданном
порядке и ничего другого, начинающегося с `#` (подсунутый из стенограммы
«### Правки автора» отклоняется, а не вклеивается), код возврата 0, не короче
60% прежнего тела и ни одной потерянной `[[ссылки]]` на источник — пропавшая
ссылка означает выброшенный факт, и ревизия отклоняется целиком, поэтому
промпт велит помечать отменённое ⚠️, а не удалять. Ссылки сравниваются так,
как их резолвит граф (`[[Люди/Имя]]`, `[[Имя.md]]` и `[[имя]]` — один узел).
Досье без раздела «## Источники» собрано руками и не трогается никогда.
Стенограммы, минутки и «## Правки автора» не трогаются ни в каком режиме;
перед каждой правкой — бэкап в `Досье/.backup/<дата_время>/` (секунды в
имени: второй прогон никогда не затирает копию до первого).

**Протокол участникам** (`scripts/protocol.py`) — то, что после встречи
отправляют людям: суть одной строкой, решения, поручения, открытые вопросы и
риски, собранные из Саммари и Минуток. Вики-ссылки превращаются в имена, значки
автотезисов убираются, пустые секции не печатаются. `--style plain` даёт текст
для письма и мессенджера (без markdown), `--copy` кладёт его в буфер обмена,
`--out` — в файл. Стенограмма в протокол не попадает ни при каких флагах:
разослать участникам сырую расшифровку опаснее, чем не разослать ничего.

**Одна встреча — одна папка** (`scripts/dedup_archive.py`) — тема встречи
уточняется при повторных разборах, а папка архива называется по теме. Пока
архивация не умела переименовывать, каждое уточнение заводило вторую папку на ту
же встречу: к 03.08 таких пар накопилось на 21 встречу из 62. Причина закрыта в
`meeting_archive` (папка переименовывается), а накопленное разбирает этот скрипт:
остаётся папка, чьё имя совпадает с нынешней темой в графе, файлы из лишних
переносятся в неё, сами лишние уезжают в `Встречи-архив/_дубли`. Без `--apply` —
только показ, ничего не удаляется никогда.

**Слияние графов** (`scripts/merge_graphs.py <донор> <приёмник>`) — сшивает
расколовшийся граф: 03.08 рабочая встреча уехала в новый граф, который
модель честно придумала по содержанию. Конвейер расколы теперь
предотвращает (список известных графов в промпте), а уже случившиеся
сшивает этот скрипт: новые файлы переносятся, коллизии имён дописываются
в файл приёмника секцией «Перенесено из графа …» (frontmatter донора
срезается — узлы людей и систем аддитивны, терять их истории нельзя),
строки встреч переезжают в `_MOC.md` приёмника, `_MOC.md` донора
становится пометкой «слит в …». Без `--apply` — только план. Применение до
первого переноса проверяет все операции, автоматически склеивает только
Markdown-коллизии, сохраняет резервную копию и при любом позднем сбое восстанавливает
все затронутые файлы.

**Забыть встречу** (`scripts/forget_meeting.py <дата|штамп>`) — обратная
сторона записи: убирает встречу из всех шести мест, где она живёт —
стенограмма и производные, папка в «Встречи-архив», узел «Встречи/», копия
стенограммы в «Документация», строки хроники в Ядрах (вместе с фактом, который
пришёл из этой встречи) и ссылки в Досье и узлах людей. Ссылка внутри фразы
заменяется пометкой «(встреча удалена)», а не оставляет ссылку в пустоту. По
умолчанию печатает план: удаление необратимо и требует `--yes`. Правки узлов,
которые остаются жить, бэкапятся в `.forget_backup/<штамп>/` — удаляем встречу,
а не чужие заметки. Только стенограмма и запись, без графа — `--keep-graph`.

**Ночной контур** (`scripts/nightly.sh`, повесьте в cron/launchd): **утренний
бриф** (ранний) → Tier3-ревизия → **досье** → *(опционально)* **ревизия досье
облаком** → дедуп файлов → **утренний бриф** ещё раз → **бенч памяти**. Бриф
пишется дважды: он стоит секунды и не зовёт модель, а ревизия на большом графе
идёт часами — и однажды ночь кончилась раньше неё, оставив человека утром с
вчерашним `_Сегодня.md`. Ранний проход даёт бриф при любом раскладе, поздний
переписывает его поверх причёсанных ядер и свежих досье. Ночная ревизия сливает
ядра только при `sufler.tier3_auto_apply: true` — без этого ключа она
ограничивается обратимыми пометками; право на необратимое живёт в
конфиге, а не в расписании. Облачная ревизия ядер (`nightly_claude_cores.py`)
берёт в промпт не «первые по алфавиту», а сначала ядра, изменившиеся с
прошлого прогона, потом остальные — по давности показа, самые давние первыми (одно место
такому ядру — всегда), целыми, без обрыва посередине; что ушло в облако, запоминается в `logs/nightly_cores_seen.json`,
и лог ночи называет честный охват («ядер 35 из 161»). До 22.08 при сотнях
свежих ядер ревизия видела ~4% корпуса, ночь за ночью одни и те же. Утренний бриф
(`scripts/morning_brief.py`) собирает в каждом графе файл `_Сегодня.md` —
последние встречи с сутью, «Решили»/«Поручения»/«Открыто» из Саммари,
живые ядра и пометки на сведение; всё из готовых строк графа, без LLM —
утренний контекст за минуту чтения ДО первого вопроса. Бенч памяти
(`scripts/memory_bench.py` + `config/memory_bench.yaml`, формат в
example) гоняет эталонные вопросы через реальный RAG-контур и проверяет
обязательные факты в ответах — деградация после твиков порогов/промптов
видна в ночном логе, а не на живой встрече. Шаги независимы: упавшая
ревизия не отменяет бриф, но прогон завершается ненулевым кодом — раньше
джоба всегда кончалась `echo`, и launchd показывал зелёное даже когда
ночью не делалось ничего. Просевший бенч — предупреждение в логе, не
авария: это сигнал деградации, а не поломка контура.

- **Время встречи в имени папки архива** — «2026-07-24 11-30 — Тема»:
  пять встреч в день различимы с одного взгляда, а mtime папки после
  доработок всё равно врёт. Папки старого формата мигрируют
  автоматически при следующем обновлении.

## Вне встреч

- **Отказ обновления виден, а не молчит** — helper подмены бандла отказывается работать, пока приложение живо (защита записи), но его exit 75 уходил в /dev/null: обновление молча не устанавливалось, человек узнавал по неизменившейся версии. Теперь отказ оставляет маркер, и при следующем старте строка обновления честно говорит, что произошло и что делать. Записи компаньонов (iPhone/Android) больше не могут молча перезаписать друг друга при коллизии имени в одну секунду; события календаря без даты не роняют список; статусы диктовки, импорта и чата локализованы; загрузка моделей Ollama идёт мимо системного прокси — четвёртое и последнее место грабли 13.08.

- **Первый запуск без терминала** — экран онбординга показывает проверку
  готовности (окружение, конфиг, Ollama, модели, микрофон, папка графа), и
  непройденный пункт чинится на месте: недостающую модель приложение
  скачивает само через API Ollama, с процентами из его же стрима; рецепты,
  которые кнопкой не чинятся, копируются в буфер целиком, а не
  перепечатываются с экрана. Кнопка «Начать слушать» разблокируется, когда
  блокирующих пунктов не осталось.
- **Диктовка** (глобальный хоткей) — наговорили → распознано локально →
  вставлено в активное поле; буфер обмена восстанавливается, включая
  картинки. Текст уходит в то приложение — а при праве Accessibility и в
  то окно, — где начали диктовать: распознавание идёт секунды, и если
  впереди уже другое, текст остаётся в буфере (прежнее содержимое буфера
  не восстанавливается), плашка и статус об этом говорят — нажмите ⌘V в
  нужном поле. Старт из меню самого Чароита — как раньше, текст идёт туда,
  куда кликнули. Пароли: если диктовка хоть раз коснулась поля пароля (или
  оно под фокусом в момент доставки), ничего не вставляется и не
  показывается — текст ждёт в буфере, плашка и статус несут только
  инструкцию; без права Accessibility поле пароля не отличить, поэтому
  живая плашка не показывается вовсе. Каждый кусок живого черновика
  попадает на плашку только по чтению фокуса, запущенному после его
  прихода (межпроцессный запрос, миллисекунды): клик в поле пароля по
  ходу речи гасит её со следующим куском или сторожем раз в секунду, и до
  конца диктовки она не возвращается; если приложение впереди не
  отвечает, плашка молчит. Поле пароля распознаётся через Accessibility:
  приложение, не отдающее своё дерево (браузеры на Chromium с выключенной
  accessibility), отличить не позволяет — там плашка и вставка работают
  как в обычном поле. В меню-баре диктовка, заметка и дневник стоят столбиком, а
  сочетания клавиш — отдельной колонкой справа: в строку они не влезали и
  обрезались как раз на сочетании.
- **Живой черновик диктовки** (macOS 26+) — пока вы говорите, системный
  движок на устройстве показывает черновик на плашке внизу экрана (она не
  крадёт фокус — вы диктуете в чужое поле). Черновик — не результат: на
  эталоне 02.09.2026 он дал 12,4 % ошибок против 2,9 % у GigaAM и теряет
  термины, поэтому итоговый текст всегда от GigaAM после стопа. Черновик
  идёт в дело только если python запустился, но распознать не смог (нет
  модели, сломанный импорт) — диктовка на маке без модели всё равно
  работает, строка статуса об этом говорит. Сторож даёт распознаванию
  25 с плюс пятую часть длины записи (десять минут речи при 26x — это
  ~23 с, на занятой машине до сотни), потом просит выйти и через десять
  секунд добивает; если до этого дошло, черновик точно так же идёт в
  дело, а чей это текст, говорят строка статуса и плашка.
  У заметки и дневника плашки нет, и черновик они не берут никогда: их
  текст уходит в граф и память, там точность важнее мгновенности. Ассеты
  речи приложение не скачивает (рубильник и PRIVACY.md остаются правдой):
  если язык диктовки не установлен в Системных настройках → Клавиатура →
  Диктовка, черновика просто нет.
- **Импорт записанных встреч** — `scripts/import_meeting.py файл`
  (аудио m4a/wav/mp3, текст txt/md, субтитры vtt/srt из Zoom/Teams —
  имена спикеров сохраняются) → полный архив встречи: стенограмма,
  минутки, разбор, тезисы, граф; дата встречи — `--date/--time`;
  исходник остаётся рядом с материалами встречи (APFS-клон).
- **Нить встречи** — главное, что видно во время разговора. Она не
  пересобирается каждые несколько минут, а **растёт**: модель получает уже
  собранное и дописывает только новое, а если нового нет — молчит, и на
  экране ничего не дёргается. Темы служат якорями, под ними копятся строки,
  и вид строки виден знаком: `●` тема, `-` что сказали, `⚑` решение или
  срок, `?` открытый вопрос, `⏮` что было по теме раньше (с датой, из
  архива). Ранние темы сворачиваются в заголовок со счётчиком — читать
  нить, а не прокручивать. Повтор той же мысли другими словами
  отбрасывается кодом: сверяются и посимвольная похожесть, и пересечение
  значимых слов. Зовём модель по приросту разговора, а не по часам:
  молчание в переговорной новых строк не рождает. **Нить — единственное полотно на экране**: ответы (⚡ мгновенный, ☁ облачный) и тезисы (📌 решение, 💭 мысль) демон вплетает в неё строками, а не держит в своих панелях. Пока панели жили порознь, автоответ приходил раз в полминуты, панель подсказки не пустела никогда — и нить, ради которой всё затевалось, не показывалась вовсе. Кнопка ⏮ и хоткей `⌘⇧E`
  убраны из панели вместе с тезисным контуром (пакет владельца 24.08);
  строки ⏮ из архива остаются частью формата нити для headless-запусков. **Имя говорящего — только при смене
  голоса**: «Собеседник 4:» на каждой строке читался как протокол допроса;
  теперь реплики одного человека идут подряд без префикса, имя появляется
  в момент передачи слова. **Облако правит нить, а не комментирует её
  сбоку**: раньше уточнение приходило отдельным блоком «☁️ …», и его надо
  было самому сопоставлять с полотном; теперь ревизор возвращает пары
  «строка → как точнее», строка правится на месте, изменённые слова
  выделены ==так== (в приложении — небесным: облачное видно), а полный
  след «было → стало» уходит в файл-лог встречи.
- **Подсказка ведёт нить разговора**, а не отвечает за вас: тема сейчас
  → кто что сказал → **почему** это обсуждают → **что было** по теме в
  архиве (с датой) → что осталось открытым. Пустые разделы пропускаются,
  а несколько тем в одном куске разговора разбиваются на отдельные
  карточки. Формат живёт в коде (`src/llm.py`, `HINT_FORMAT`) и одинаков
  для всех: роль в конфиге описывает контекст и термины, а не разметку.
  Имя говорящего — только при передаче слова, без отчётных глаголов («утверждает», «уточняет») — суть, не пересказ. Вызывается по `⌘⏎`. Готовый ответ от
  первого лица никуда не делся — он на отдельной горячей клавише.
  **На экране подсказка — карточка НАД нитью, а не вместо неё** (#255
  чинился дважды, потом №22): прежний выбор «или-или» позволял одной
  подсказке спрятать нить до конца встречи, а «Стоп» стирал итог с
  экрана — теперь нить на месте, авто-подсказка живёт на карточке минимум три
  минуты (раньше гасла первым же обновлением нити — за полминуты её не
  успевали дочитать), отгоревшая своё уступает нити, а ответ на ЗАДАННЫЙ
  человеком вопрос живёт, пока его
  не вытеснит следующая подсказка или не закроют крестиком на карточке
  (тик нити приходит через секунды после ответа — он не смеет его съесть).
  Идущий стрим подсказки нить тоже не режет: демон помечает стримы как
  ручные/авто, и приложение их различает. Лента и токены подсказки
  обновляют экран пачками (лента — до 4 раз в секунду, стрим — ~7 кадров),
  а не на каждый кусок распознавания: плотная речь не съедает
  отзывчивость кнопок. Во время стрима панель держится
  за верх карточки — токены не уезжают мимо читателя. Если подсказки выключены,
  пустая панель называет причину, а не обещает нить, которая не вырастет.
- **Живой контекст встречи** — тема проявляется в первые минуты
  разговора; демон выжимает её из живой стенограммы, ищет по архиву и
  пересобирает блок «память прошлых встреч» в промпте подсказок:
  подсказки, мгновенные ответы и вопросы видят старые договорённости
  по ТЕКУЩЕЙ теме, а не просто две последние встречи. Локально,
  включено по умолчанию (`sufler.live_context`).
- **Сверка с узлами графа на живой встрече** — прозвучал человек, система
  или ядро, у которых есть узел в графе, — его история (последние записи
  «## Встречи»/«## Хроника», статус) сама приходит строками ⏮ в текущую
  тему нити: старые договорённости видны, пока разговор идёт. Без
  brain-сервера и без LLM — стемы имён против файлов графа
  (`src/graph_nodes.py`). Автоматика строгая: многословное имя должно
  собраться целиком, однословное — прозвучать дважды (кроме кодов с
  цифрами и уже опознанных спикеров), имя на два узла молчит, не больше
  четырёх узлов за встречу. ⚡-вопрос сверяется мягче: узлы из самого
  вопроса добавляются в промпт мгновенного ответа. Те же узлы — фолбэк
  живого контекста и ручного ⏮, когда brain-сервер не поднят.
- **Облачное уточнение подсказок** — лестница и для подсказок:
  локальная модель отвечает мгновенно, затем облачная (Claude CLI,
  по умолчанию Haiku) дописывает «☁️ …» прямо в ТУ ЖЕ карточку
  подсказки (устаревшие уточнения — только в лог подсказок). Шлёт
  стенограмму в облако на каждую подсказку — отдельный выключатель,
  по умолчанию ВЫКЛ (`sufler.cloud_hints`).
- **Стартовый бриф** — карточка подсказок не пустует в начале встречи:
  тема прошлой встречи, договорённости и число открытых задач из
  архива — мгновенно, файловый парс, только основной граф.
- **iPhone-компаньон (v1, app-ios/)** — телефон записывает
  встречу/заметку/дневник; папку доставки выбираете один раз в iCloud
  Drive (кнопка-папка вверху) — та же папка импорта, за которой следит
  Mac. Звонок или ВКС посреди записи — это пауза, а не поломка: микрофон
  на iPhone принадлежит звонку (ограничение iOS), приложение говорит об
  этом прямо, ждёт конца разговора и продолжает ТОТ ЖЕ файл; попытки
  «поднять» запись и ротация файла на время звонка подавлены — 07.08
  именно они превратили 30-минутную встречу в 40-килобайтный огрызок.
  **Слушает сразу (№167)**: открыли приложение — запись уже идёт
  (настройка «Писать сразу при открытии», по умолчанию включена, тип —
  последний выбранный, один раз за запуск, не при возврате из фона);
  открыли во время звонка — приложение взводится и стартует само, когда
  звонок отдаст микрофон, пока приложение открыто (свёрнутое iOS не
  запустит; возврат в течение 30 минут — старт при возврате); автостарт
  ждёт выбранной папки доставки; App Intent «Начать запись в Charoite» для Siri,
  Команд и кнопки действия открывает приложение и стартует; манифест
  приватности PrivacyInfo.xcprivacy добавлен. Из фона iOS запись не
  запускает, сам звонок не пишет — это платформа, не приложение.
  Папку вне iCloud выбрать нельзя: локальное хранилище телефона
  выглядит в «Файлах» так же, но записи из него никуда не синкаются —
  закладка на такую папку отклоняется с внятной ошибкой, а старые
  локальные закладки забываются, и приложение просит выбрать заново. Недоставленное не пропадает: очередь в Documents с досылкой при
  каждом запуске. Файлы `note_*`/`diary_*` уходят в конвейер заметок.
  Фоновая запись при заблокированном экране. Сборка: `xcodegen generate`
  в app-ios/, подпись в Xcode.
- **Запись можно забрать руками** — не дожидаясь iCloud и не завися от
  него: кнопка «Поделиться записью» под таймером отдаёт последний файл
  куда угодно, а папка записей видна в «Файлах» на телефоне и по кабелю
  (`UIFileSharingEnabled`). Пять последних записей остаются на телефоне и
  после доставки: «iCloud принял» не значит «Mac получил».
- **Очередь видно целиком** — строка «в очереди: 6» открывает список: что
  записано, когда, сколько весит, сколько ждёт. Записи старше суток
  подсвечены — обычная доставка занимает секунды, и всё, что висит дольше,
  висит уже не «сейчас уедет». Оттуда же досылка одной кнопкой и передача
  любой записи руками.
- **Сторож вставшей записи** — если длительность файла перестала расти
  дольше трёх секунд (прерывание, отобранный микрофон), экран говорит об
  этом оранжевым, а не рисует бегущий таймер поверх тишины.
- **Разбор переживает вставшую модель** — Ollama умеет застревать так, что
  снаружи выглядит здоровой: список моделей отдаётся мгновенно, а запрос
  висит до таймаута. Перед разбором идёт дешёвая проба генерацией; вставшая
  локальная Ollama перезапускается сама (только loopback — чужую машину не
  трогаем), после чего разбор повторяется. Перезапускается именно тот
  процесс, который держит порт: приложение и brew-сервис уживаются на одной
  машине, и лечить надо того, кто отвечает на запросы.
- **Незавершённые встречи догоняются** — упавшая или брошенная обработка не
  остаётся лежать: следующая удачная встреча подбирает самый свежий хвост и
  повторяет его (до трёх попыток, идущую обработку не трогает). Раньше сбой
  означал молчание — встреча просто не появлялась в графе.
- **Забытая запись останавливается сама** — 17.08 встреча кончилась, «Стоп»
  никто не нажал, и ноутбук писал пустую комнату 18 часов 25 минут. Теперь
  запись заканчивается сама: речи не было ни разу — через 5 минут, говорили и
  замолчали — через 15 (в живом разговоре пауза на чтение документа и тихое
  демо нормальны), и в любом случае через 6 часов — потолок длительности.
  Тишина считается по РАСПОЗНАННОЙ речи и только по новым строкам стенограммы,
  а не по громкости: кулер и клавиатура разговором не считаются, а
  повторяющаяся фантазия распознавания на шуме съедается дедупом и таймер
  тишины не сбрасывает. Обратная сторона названа честно: громкая фоновая речь —
  телевизор, радио, видео — для нас разговор, такую запись остановит только
  потолок. Число говорящих в правиле не участвует намеренно: без моделей
  диаризации все реплики очной встречи идут одной меткой, и «один голос»
  ничего не значит. За минуту до конца приходит предупреждение (а если окна
  не видно — баннер), любая реплика его снимает. Останавливается запись тем же
  путём, что по кнопке «Стоп»: файл финализируется, стенограмма пересобирается,
  граф обновляется — потерять нечего; в статусе после остановки написана
  причина. Первые две минуты записи не трогаются вовсе («включил, пока все
  собираются»). Прощание в живой стенограмме («всем пока», «до свидания»)
  ускоряет остановку: после одного порог тишины падает до минуты
  (предупреждение приходит сразу, любая реплика его снимает), два прощания
  подряд — обмен «пока-пока» — останавливают запись немедленно. Детектор
  детерминированный и нарочно узкий: короткая прощальная реплика, а не союз
  «пока» внутри фразы. Всё настраивается в `sufler.autostop`
  (`farewell_seconds`, 0 выключает), `autostop: false` выключает целиком.
- **Запись без речи названа своим именем** — сорок секунд тишины это не
  «ошибка обработки», а результат: статус так и говорит, стенограмму можно
  открыть, а повторять разбор тишины конвейер не станет. Незнакомое
  состояние из более новой версии конвейера приложение тоже переживает —
  раньше оно роняло разбор статуса, и встреча пропадала из окна целиком.
- **Раздел «Встречи» (бывшее окно «Последние»)** — двадцать встреч за две недели: состояние
  цветом, «Открыть» и «Стенограмма» у каждой строки, у упавшей — кнопка
  «Повторить». У готовой встречи в строке видна длительность (по
  таймкодам стенограммы, с кэшем — файл перечитывается только когда
  встречу дорабатывали). Пока встреча повторяется, «работаю» показывается только у
  неё: готовых строк чужой повтор не касается. Пункт меню-бара «Последние
  встречи» открывает этот раздел workspace и виден даже при пустой истории —
  список честно объясняет пустоту сам; отдельного окна больше нет (№22),
  полный архив живёт в папке «Встречи-архив». Поле поиска в шапке отвечает на «где мы
  решали про X»: лексический поиск по саммари, минуткам и разборам архива
  (и заметкам графа для недоехавших встреч), новые первыми, клик открывает
  документ. Стенограммы не сканируются: решения к этому моменту уже вынесены
  в верхние слои. Поиск в библиотеке видит и УЗЛЫ графа: запрос «имя
  человека» ведёт к его узлу со всей историей (совпадение по имени —
  первым, стемами: «платежного» находит «Платёжный шлюз»), совпадения в
  теле узлов — после встреч; узел откроется файлом графа.
- **Запись с экрана «Сегодня»** — та же капсула-кнопка, что и в суфлёре по духу: старт/стоп с таймером и живой волной, честный хоткей ⌘⇧␣ и строка готовности от реальных проверок установки (Python, микрофон, системный звук, Ollama, модели, граф) — с числом пройденных проверок и первой проблемой чипом. Готовый вчерашний результат больше не прячет кнопку следующей записи: «Открыть результат» и капсула живут рядом. Во время обработки капсулы нет сознательно: запись и конвейер конкурируют за локальные модели. Первый запуск ведёт в онбординг, а не сразу в запись.
- **Карточка встречи** — готовая встреча открывается по клику на строку:
  тема, дата, длительность (по таймкодам стенограммы), участники, суть
  одной строкой, решения и поручения. По умолчанию карточка показывает
  **подробный протокол из Минуток**: темы разговора, полные формулировки
  решений, поручения со сроками, открытые вопросы и риски — то же, за чем
  раньше приходилось идти в Obsidian. Переключатель «Подробно / Коротко»
  возвращает выжимку из Саммари; если встреча ещё шла, подробный вид
  честно помечен черновиком. Разметку минуток модель пишет по-разному
  (`- Темы:`, `**Темы:**`, `## Темы обсуждения`) — разбор понимает все
  варианты. Если по встрече шла
  облачная ревизия, карточка честно показывает её итог из лога: «правок
  графа — N», а несохранившийся файл ревизии подсвечивается — раньше
  ревизия работала невидимкой, и о правках узнавали только из логов. «Копировать» кладёт в
  буфер резюме, задачи или всё сразу — в письмо уходит без открытия
  файлов. Кнопки «Открыть», «Стенограмма» и «Obsidian» — на месте.
  Результат виден в приложении: «встреча готова» больше не значит «идите
  разбираться с markdown-файлом».
- **Переименование встречи** — карандаш в карточке или
  `scripts/rename_meeting.py <штамп> "Новая тема"`. Тему придумывает
  модель, и она бывает мимо; поменять руками значило обойти пять мест.
  Скрипт разносит тему по всем: файлы transcripts/, архивная папка (и
  ссылки внутри неё), копии в Документации, заголовок заметки графа,
  статус приложения. Главный файл без темы — включая посекундный из
  встреч, разобранных до фикса конвейера, — получает её тоже: до этого
  такая встреча показывалась в списке датой вместо темы. Свежие
  посекундные стенограммы конвейер называет сам: секунды в имени файла —
  не тема, главный файл и производные приводятся к минутному виду со
  слагом (файлы соседней встречи той же минуты не трогаются и не
  затираются). Две встречи одной минуты (демон после краха поднимается
  внутри той же минуты) в графе живут порознь: минутный ключ — у самой
  ранней, вторая получает посекундный (`Встречи/2026-08-21_125812`,
  папка архива «12-58-12 — …»); правило одно — `meeting_stamp.graph_key`,
  его же держат поиск заметки, «забыть» и переименование (штамп с
  секундами выбирает вторую). Старая тема остаётся в
  aliases — поиск по ней не слепнет; ссылки [[Встречи/штамп]] не рвутся,
  тема в них не участвует. Без `--yes` скрипт печатает план и ничего не
  трогает.
- **Экран «Подготовка»** (меню-бар → Сегодня) — помощь ДО встречи, а не
  после: оставшиеся события дня из календаря (тот же opt-in-доступ, что у
  подсказки о записи), «что было по теме» — три находки из архива по
  названию ближайшего события (служебные хвосты вида «(еженедельно)»
  отрезаются), открытые поручения и карточка прошлой встречи. Всё
  собирается за минуту до начала, без похода по четырём окнам.
- **Единый рабочий стол встречи** — главное окно macOS теперь содержит
  разделы «Сегодня», «Встреча», «Встречи», «Задачи» и «Память» в боковой
  навигации. «Сегодня» ведёт от ближайшего события через запись и обработку
  к готовому результату. В библиотеке список и карточка стоят рядом, а
  поисковая находка открывает карточку, не сырой Markdown. Меню-бар и
  подготовка ведут в тот же рабочий стол; отдельный чат остаётся только
  необязательным выносным окном.
- **Библиотека встреч — лента карточек** (ревизия августа 2026, экран 3
  macOS-макета) — колонка «Встреч» сгруппирована по дням: Сегодня · На этой
  неделе · Раньше, над ней сводка («8 встреч · 1 собирается · 1 с ошибкой» —
  у каждого числа источник: состояние конвейера). У карточки точка
  состояния (собирающаяся встреча пульсирует индиго), длительность,
  участники и поручения числами с источником, суть и мини-сегмент глубин
  чтения — Резюме · Минутки · Разбор · Стенограмма: клик открывает карточку
  сразу на этой глубине, отсутствующая глубина — пунктирный чип. Упавшая
  встреча говорит, что случилось, словами («Ошибка — исходник сохранён:
  Ollama не ответила за 300 с») и предлагает «Повторить обработку» на месте;
  запись без речи — результат, а не ошибка. Поиск без находок — общий
  EmptyState со «Сбросить поиск». Лента — кнопки в ScrollView, а не List
  с selection: тот же выбор, что у сайдбара, где List терял клики (а его собственный ScrollView позже рисовал строки на две позиции ниже кликабельных мест — теперь сайдбар это чистый VStack: нарисованная строка и есть кликабельная).
  Группировка, сводка и склонения — чистая `LibraryScreenPolicy`,
  тестируется без UI.
- **Экран «Память» показывает, откуда ответ** (ревизия августа 2026,
  экран 5; гамма — библиотеки встреч, решение владельца) — ответ модели
  это белая карточка с волосяной рамкой, а под текстом — чипы источников,
  разобранные из контекста поиска по архиву: встречи — залитые
  индиго-капсулы (клик открывает карточку встречи в библиотеке), узлы и
  досье — контурные (клик открывает файл). Ниже — строка происхождения:
  «локально · модель · секунды · N встреч в контексте», при выключенном
  тумблере графа — «память выключена», а при слабых совпадениях — чип
  «⚠ совпадения в графе слабые» вместо чипов (источники не выдумываются).
  В шапке — живой чип «Ollama отвечает · 0 запросов в сеть», который
  становится предупреждением, когда локальная модель молчит. На окнах
  ≥760 pt справа — колонка «Что знает память»: счётчики встреч/узлов/досье
  и свежие ядра со строкой статуса, посчитанные по графу на диске не чаще
  раза в минуту. Разбор источников, подписи и мета-строка — чистая
  `MemoryScreenPolicy`, тестируется без UI.
- **Телеметрия текстовых пар для подписи владельца** (24.08) — когда
  встреча звучит из колонок, эхо собеседников попадает в микрофон, и
  владелец оставался «Собеседником N» на каждой живой встрече с
  динамиками: сверка по voice id слепа (live-трекер размечает каналы
  независимо, id эха в микрофоне не совпадает с id собеседника в
  системном канале). Демон теперь НАХОДИТ текстовые пары — фраза, чьи
  уникальные слова перекрывают ≥0.8 недавней фразы другого канала, от
  пяти слов, окно пары 8 секунд, любой порядок прихода — и считает их
  по голосам. Два круга ревью показали: авто-пометка поверх этого
  (один хит, два хита, гвард владельца) ложноположительна с той или
  другой стороны шкалы времени, поэтому счётчики сознательно НЕ влияют
  на подпись: они питают строку owner-pulse раз в минуту (только
  агрегаты — числа голосов, доля лидера, счёт эха и пар, вердикт;
  пер-голосовые данные на диск не попадают — обещание «ничего
  голосового на диск» держится). Включение пометки — отдельное решение
  поверх полевых данных реальных встреч. Буферы фраз живут только в
  памяти процесса.
- **Память по графу — один клиент** (24.08, партия D-П3 карты
  оздоровления) — три ручных обращения демона к brain-серверу слиты в
  src/brain.py: одно тело запроса, folder графа проекта в одном месте;
  таймауты и деградация остаются за контурами (⚡ 2.5 с, дежавю 8 с,
  глубокий 6 с). Вопрос владельца в подписи аудита больше не режется на
  120 знаках — потолок 400 (№95).
- **Тяжёлый фон координируется, а не сталкивается** (24.08) — в ночь
  23→24.08 ручной прогон мутатора делил единственную локальную модель со
  встречей и ночным циклом: досье поймали 35 ReadTimeout по 300 секунд.
  Теперь у тяжеловесов общий словарь занятости (busy_signals): мутатор
  не стартует при записи, разборе встречи или бегущей ночи (честный
  отказ, --force для настойчивых), держит эксклюзивный flock на весь
  прогон — тот же механизм ядра, что замок встречи у демона, поэтому
  убитый процесс отпускает его мгновенно — и уступает между мутантами,
  как только начинается запись или ночь; ночь, в свою очередь, ждёт лок
  мутатора так же, как ждёт разбор встреч, и обновляет свой статус на
  каждом шаге — длинная ночь не становится невидимой.
- **Слой подсказок объясняет своё молчание** (24.08) — три встречи подряд
  авто-подсказки молчали при живых минутках и дежавю, и мёртвый цикл был
  неотличим от «нечего сказать». Теперь слой наблюдаем и лечит себя сам:
  строка hint-pulse раз в минуту в err-логе (включён ли, сколько нового
  разговора, исход последней генерации, серия сбоев), строка cmd-in на
  каждую полученную команду панели (команды с содержимым логируются
  только словом и длиной — контент владельца в err-лог не попадает),
  сторож в главном цикле перезапускает умерший поток подсказок (до трёх
  раз, дальше честное «падает повторно» с напоминанием раз в пять минут),
  а stall-детектор сообщает о потоке, который жив, но не тикает два
  цикла — случай «завис в генерации», невидимый проверке живости. Только
  состояние, без контента стенограммы; доезжает само со следующим стартом
  встречи — демон живёт ровно одну встречу.
- **Панель суфлёра без лишних слов** (пакет владельца, 24.08; вечером
  того же дня партией G-П1 карты оздоровления удалён и сам тумблер
  thesesOn — слой глушится несущей quiet-посылкой) — ⚡-ответ
  на вопрос собеседника — одна выделенная строка нити (полужирно, цветом
  действия), без строки самого вопроса: вопрос остаётся в аудите
  `_hints.md`, полотну он не нужен. Тезисный контур из панели убран (чип
  и кнопка ⏮; демону слой глушится безусловно), кнопка ручного запроса
  переименована в «Конспект» — рядом живёт чип слоя «Подсказки», и две
  одинаковые надписи читались как дубль. Конспект пишет короче: до двух
  строк на тему, каждая до 12 слов.
- **Подсказчик не умирает молча** (24.08) — авто-цикл подсказок держит под
  try весь шаг итерации (раньше исключение до внутреннего try убивало поток
  навсегда: встреча 24.08 прошла без единой авто-подсказки при живом
  heartbeat), после трёх сбоев подряд говорит это статусом; замок
  подсказчика все восемь держателей (авто, ручная, ⚡, минутки, нить, ⏮,
  глубокий разбор, тема для архива) берут с потолком ожидания — зависший
  сосед больше не обездвиживает остальных навсегда, человек получает
  «подсказчик занят», а не тишину; сигнал «уступи» ручного запроса гаснет
  и при таймауте, иначе авто-подсказки вечно уступали бы несуществующей
  ручной.
- **Календарь внутри библиотеки встреч** — полоса недели живёт над списком
  «Встреч»: точки отмечают дни с записями, один клик превращает список в
  ленту дня — записи остаются обычными выбираемыми строками, а события
  календаря, которые никто не записал, показываются приглушёнными строками
  («14:00 · без записи», «идёт», «впереди»). Запись прикрепляется к
  событию, если началась в его окне (за пятнадцать минут до начала — уже
  считается; две встречи подряд одну запись не делят); событие с записью
  второй раз не показывается — запись сама говорит за встречу. Активный
  поиск главнее фильтра дня, без доступа к календарю полоса навигирует по
  одним записям, подключение — маленькая кнопка на самой полосе. Пустая лента
  без разрешения честно говорит, что события недоступны, а не объявляет день
  пустым; изменения EventKit обновляют уже открытый день без перезахода. Один
  раздел отвечает и на «что решили?», и на «что было во вторник?» —
  отдельного экрана «Календарь» больше нет.
- **Действия над встречей без терминала** — карточка копирует безопасный
  протокол через `protocol.py`, открывает стенограмму для исправления,
  пересобирает результат и до необратимого подтверждения показывает все
  следы, которые удалит `forget_meeting.py`. Правила приватности по-прежнему
  реализованы один раз — в проверенных скриптах. Разрушающие действия
  подсвечены красным, у переименования есть отмена (крестик или Esc), а
  протокол в стиле «для мессенджера» уходит без markdown-разметки.
  «Пересобрать результат» говорит, что произошло на самом деле: запущена
  (если записи ещё хранятся, стенограмма распознаётся заново, прежняя версия
  уходит в transcripts/.prev; правленную руками стенограмму заново не
  распознаём — минутки собираются по вашему тексту, если их самих не
  правили руками, иначе только перештамповываются; повторный клик без
  изменений ничего не делает; хеш последней машинной записи лежит в
  live.json, а `CHAROITE_FORCE_STT=1` при пересборке из терминала
  распознаёт заново, правки уходят в .prev), уже идёт пересборка или повтор другой
  встречи, стенограммы нет на диске или не удалось запустить (отсутствующий
  python ловится до старта процесса) — раньше на любой клик писалось
  «запущена», включая молчаливые отказы; пункт гаснет на весь прогон
  пересборки этой встречи, строка сбрасывается при переходе к другой
  встрече, а процесс, умерший после старта, превращает её в текст отказа.
  Дата и
  время на карточке — начало самой встречи (штамп записи), а не момент,
  когда конвейер дописал статус.
- **Переносимая карточка встречи** — архив создаёт восстанавливаемый
  `meeting.meta.json` со стабильными, не зависящими от языка ключами для
  участников, сути, решений, поручений и открытых вопросов. Markdown остаётся
  источником истины, манифест пересобирается из него. macOS, iOS и Android
  показывают одинаковую структурированную карточку, а старые встречи без
  манифеста продолжают открываться из заметок графа.
- **Поручение не теряет свою встречу** — карточка результата и подготовка
  показывают живые чекбоксы из `Минутки.md` именно этой встречи; отметка сразу
  пишет `[x]` обратно в Markdown. «Все задачи» открывает раздел уже с фильтром
  по встрече, а заголовок каждой группы ведёт обратно к её карточке. В том же
  окне есть поиск, фильтр сделанных и копирование видимого списка. Если между
  сканом и кликом Obsidian сдвинул строки, Чароит находит одно однозначное
  поручение или отказывается писать — соседняя задача не будет закрыта
  случайно. Завершившийся конвейер сам обновляет список.

- **Пересборка всегда находит свои записи** — имя встречи собирается одним
  модулем на весь конвейер: демон называет записи каналов, пересборка по ним
  ищет, retry из приложения доводит минутное имя с темой до посекундного
  штампа записей. Оба канала разрешаются вместе: если микрофон и системный звук
  указывают на разные встречи в одной минуте, пересборка отказывается от них,
  а не выпускает смешанную стенограмму. Разъезд формата 28.07 молча лишал финальной стенограммы
  все встречи подряд — теперь договор об имени держат сквозные тесты.
- **Модели под вашу машину** — мастер первого запуска читает объём памяти и
  показывает три набора («Полный», «Сбалансированный», «Лёгкий») с именами
  моделей и пометкой рекомендованного; выбранный записывается в конфиг и
  качается одной кнопкой. Раньше пресеты лежали комментарием в конфиге, и
  человек сам искал свою строчку — ошибка была молчаливой: тяжёлая модель не
  падает, а уходит в своп, и продукт кажется медленным.
- **Python-контур внутри приложения** — Charoite.app несёт переносимый
  CPython с рантайм-зависимостями (346 МБ; тяжёлые опциональные пресеты STT в
  поставку не входят). Из установки уходят `git clone`, `venv` и `pip`:
  терминал нужен только для Ollama. Из исходников всё работает как раньше —
  приложение берёт вложенный контур, если он есть, и `.venv` рядом, если нет.
- **Установка без терминала и YAML** — мастер первого запуска спрашивает имя
  и папку графа и пишет их в конфиг сам, папка выбирается панелью; модель
  разделения голосов ставится кнопкой тем же скриптом, который раньше
  запускали в консоли. Редактировать `config.yaml` руками больше не нужно.
- **Системный звук из коробки (ScreenCaptureKit)** — приложение захватывает
  звук встречи средствами macOS: ни драйвера, ни пароля администратора, ни
  Многовыходного устройства. Одно системное разрешение при первой записи — и
  всё. Агрегатных устройств не создаётся вовсе, поэтому подвесить CoreAudio
  нечему (тапы это делали четырежды). На macOS 15+ микрофон приходит тем же
  потоком, и PortAudio не открывается вообще — вместе с ним уходит класс
  аварий «мёртвый стрим виснет на close». BlackHole остаётся фолбэком.
  Поток, остановленный не нами — сон, смена дисплеев при докинге, обрыв
  связи с сервисом захвата (−3805), «Стоп» в системном индикаторе записи
  экрана (−3817), — пересоздаётся сам: на те же файлы, с паузой 2→32 с, до
  пяти попыток (обычно ~1,5 минуты, в худшем случае — с пятью таймаутами
  сборки по 10 с — около двух; сторож приложения на время пересоздания не
  считает тишину аудиовхода сбоем, а сам демон и STT-пульс остаются под
  его присмотром); демон читает файлы хвостом и продолжает с последней
  позиции без единой правки у себя. Кадры считаются у нового потока, а не
  по общему счётчику; системные вызовы сборки ограничены 10 с — зависший
  сервис захвата не держит «Стоп». Второй «Стоп» человека за две минуты
  уважается: встреча закрывается той же дорогой, что и кнопкой «Стоп»
  (запись и граф сохраняются), а не остаётся идти без звука. Не восстановилось — строка статуса, системное уведомление
  «звук встречи потерян» и немедленный перезапуск записи той же дорогой,
  что у сторожа (новый захват, откат на BlackHole, если ScreenCaptureKit не
  вернулся; не больше двух таких перезапусков за встречу — третья потеря
  закрывает встречу дорогой кнопки «Стоп»: запись сохраняется, причина —
  в строке статуса), а не тишина до конца встречи (раньше `didStopWithError` только
  логировался, и на macOS 15 с потоком уходил и микрофон). Проверено живьём
  23.08: SIGKILL `replayd` посреди захвата → −3805 → поток пересоздан
  через ~4 с (пауза 2 с + сборка + секунда на проверку кадров), кадры идут.
- **Core Audio tap — снят (02.09.2026)** — второй нативный путь к системному
  звуку (приложение читает тап своим IOProc и отдаёт демону PCM-потоком)
  был проверен в бою (38.9 с записано), но сам цикл создания и уничтожения
  тап-агрегата клинит CoreAudio на macOS 26.5: после встречи динамики
  машины умолкают до перезапуска звуковой подсистемы. Выключен 07.08, а
  теперь и убран из поставки — резерв, который лишает машину звука,
  резервом не является. Осталась уборка: агрегаты, пережившие те версии или
  падение приложения, уничтожаются при запуске и при выходе — один такой
  сирота подвесил CoreAudio всей машины. Запасной путь при недоступном
  ScreenCaptureKit — по-прежнему BlackHole.
- **Второе окно рисуется так же, как первое** — ширина колонки разделов
  задана явно, поэтому окна одной сцены не расходятся в разметке: раньше
  второе окно открывалось со схлопнутым сайдбаром, подписи разделов
  исчезали, а кнопка записи и колонка недавних встреч уезжали за край.
  Панели теперь сжимаются по приоритету, а не режутся границей окна.
- **Тулбар встречи не теряет кнопки** — при нехватке ширины шапка
  прокручивается по горизонтали, а действия встречи стоят сразу за кнопкой
  записи: раньше при старте записи они уезжали за правый край и человек
  переставал их видеть. Подписи кнопок не сжимаются и не режутся никогда.
- **Единая шкала кнопок** — семь ролей (заметная, рабочая, тихая, ссылка,
  опасная, залитая опасная — только в подтверждении, иконка) и три размера вместо
  четырёх системных стилей в 48 местах. На каждой панели ровно одна заметная
  кнопка; удаление — красным, а не оранжевым просрочки; размер и положение
  кнопки не меняются ни в одном состоянии. Спека: docs/design/BUTTONS_2026-08.md.
- **Итог встречи не исчезает по «Стоп»** — нить и подсказка остаются на
  экране после остановки записи: именно в эту минуту их дочитывают, копируют
  в переписку и сверяют с поручениями. Раньше панель была привязана к тому,
  идёт ли запись, и остановка мгновенно подменяла разговор приглашением
  спросить про архив, хотя текст оставался в памяти. Панель очищает только
  следующая встреча; карточка подсказки над нитью гаснет по своим правилам
  (см. абзац о карточке), а вопросы по архиву переехали в «Память» (№22).
- **Мёртвый канал не уносит встречу** — если источник перестал отдавать кадры,
  сторож пытается пересоздать его поток, но делает это в стороне и с
  ограничением по времени: закрытие мёртвого PortAudio-стрима умеет виснуть
  насовсем, и раньше вместе с ним вставал весь конвейер — исправный микрофон
  переставал писать в ту же секунду. Теперь безнадёжный канал отключается, о
  нём говорит статус, а встреча дописывается остальными. Открытие каналов на
  старте тоже поканальное: отказ одного источника больше не оставляет встречу
  вообще без записи. Инцидент 06.08 — четыре записи подряд по 31 секунде.

- **Диагностика не ходит мимо приватности** — `doctor` спрашивает адрес LLM
  только у privacy (уважая и запрет удалённых адресов, и рубильник), а
  post-hook встречи больше не получает ключ Anthropic в окружение: облако —
  только через подписку. Каждый процесс Claude спрашивает privacy в той же
  функции, где запускается; AST-сторож доказывает управление конкретным
  вызовом, а не наличие подходящего условия в соседней ветке файла.

- **Папка импорта (watched)** — укажите папку (Настройки → Импорт или
  `--scan` в CLI): упавшие туда записи сами становятся встречами графа;
  обработанные уходят в `done/`, сбойные остаются на виду. Повтор уже
  импортированной встречи — успех, а не отказ (файл уезжает в `done/`, а
  не молотится по кругу каждые две минуты), причём повтором считаются и
  встречи, переименованные конвейером в `<штамп>_Тема.md`. Запись без
  речи сохраняет стенограмму и завершается сразу: трёхсекундный обрывок
  больше не гоняет LLM-конвейер по всему бэклогу.
- **Номер версии на виду** — Mac: в подвале сайдбара («Charoite 0.70.1»),
  iPhone: внизу настроек записи вместе с номером сборки. Версия iPhone-компаньона
  идёт тем же релизным поездом, что и Mac (release-please правит
  `app-ios/project.yml`).
- **Вкладка «Внешняя запись»** — диктофон телефона, чужая запись звонка,
  экспорт Zoom: перетащите файл (или выберите) — он копируется в папку
  импорта, оригинал не трогается, и идёт тем же конвейером, что живая
  встреча. Разобранная встреча появляется в списке встреч со статусом,
  как записанная демоном (до 0.71 импорт статуса не писал, и встреча
  жила только на этой вкладке — поле 05.09). В списке видно, что ждёт, что собралось (штамп, кнопка
  «Стенограмма») и когда копия исчезнет: обработанная копия в `done/`
  живёт `audio.import_keep_days` (по умолчанию 2 дня) и удаляется вместе с
  аудио-«Исходником» в архиве встречи; текстовые исходники (txt/vtt) в
  архиве остаются. Сбойный файл получает метку `.<имя>.import-error`,
  остаётся на месте, не удаляется и не перегоняется через STT каждые две
  минуты — повтор кнопкой «Повторить» (или `--scan --retry-failed`).
  Ретеншн идёт после каждого скана и раз в шесть часов независимо от
  тумблера слежения; копии, попавшие в `done/` до этой версии, получают
  свой срок от первой уборки, которая их увидит. Срок независим от `record_keep_days`: чужая запись не должна
  жить дольше, чем просили.
- **Словарь замен** — STT коверкает термины и фамилии;
  `sufler.vocabulary` в конфиге чинит их декларативно (без учёта
  регистра, только целые слова) — на живой встрече, диктовке, заметках
  и импорте одинаково.
- **Команда после встречи** — `sufler.post_meeting_hook`: ваша команда
  выполняется после каждой встречи (живой или импортированной) с
  `SUFLER_TRANSCRIPT` и `SUFLER_STAMP` в окружении — локальный аналог
  webhooks.
- **Дневник (⌥⌘J)** — наговорил мысль → запись в личную сферу
  `Дневник/ГГГГ-ММ-ДД.md` секцией «## ЧЧ:ММ»: текст твоим голосом с
  пунктуацией, идеи, задачи-чекбоксами (видны в окне Задачи), «Как
  сказано»; мысль о сегодняшней встрече получает ссылку на неё —
  backlink связывает сферы, текст остаётся в дневнике. Личное не
  попадает в рабочий поиск.
- **Голосовая заметка** — наговорили мысль → модель причёсывает текст,
  ставит заголовок, извлекает задачи → файл в графе (`Заметки/`) +
  заметка запоминается в памяти. Сырой текст сохраняется рядом
  («Как сказано»).

- **Китайское распознавание без Whisper** — `stt.backend: sensevoice` гоняет
  SenseVoice Small через тот же sherpa-onnx, который уже стоит ради
  диаризации: новой зависимости нет, 228 МБ, нормализация текста включена —
  числа и время приходят цифрами, а не прописью. Whisper остаётся
  мультиязычным запасным. Поставить: `scripts/get_models.py --stt sensevoice`;
  померить: `scripts/stt_bench.py --compare` (синтезированная речь — нижняя
  планка, не бенчмарк).
- **Документы на вашем языке** — `sufler.language: ru|en|zh` переключает
  минутки, саммари, мгновенный ответ и содержимое узлов графа (имена,
  статусы, темы); подсказки говорят на языке вашей роли. Архивное саммари
  было исключением: писалось по-русски при любой настройке, и английская
  встреча получала английские минутки и русскую выжимку поверх них.
  Чтение отделено от письма: новые документы идут по конфигу, готовые
  разбираются на всех трёх языках сразу — иначе переключение языка ломает
  архив задним числом и старые встречи перестают открываться. Карточка
  встречи тоже читает все три: раньше у английской встречи «Решили» и
  «Поручения» были пустыми при том, что в файле они есть.
- **Ночной цикл одной кнопкой** — Настройки ставят launchd-задачу
  на 04:15 (ревизия ядер с бэкапами, утренний бриф, бенч памяти);
  выключается той же кнопкой.
- **Бриф по календарю** (opt-in) — тумблер в Настройках: перед
  ближайшей встречей в баре появляется кнопка с её названием, один
  клик — готовый бриф по архиву. Читается только название и время
  события, локально.
- **Свой шаблон минуток** — `sufler.minutes_template` в config.yaml:
  markdown-скелет с разделами под вашу команду вместо дефолтного.
- **Задачи со встреч** — минутки пишут поручения чекбоксами
  («- [ ] **Имя** — что — срок»), окно Задачи собирает все `- [ ]`
  графа в один список с бейджем открытых; секция «Мои» — первой
  (ответственный из `**Имя**` совпал с `user_name`), даже если поручение
  давнее; открытые сворачиваются в «Старые», когда затихли: без срока —
  старше 14 дней, со сроком — просрочка на неделю и больше (свежая
  просрочка остаётся на виду в своей корзине) — чистится экран, а не
  файлы; год у срока не пишут, поэтому он берётся от даты встречи:
  «до 15.03» в сентябре — следующий март, а не 170 дней просрочки,
  февральское «до 20.02» в сентябре — просрочка, а не будущий год, а
  срок за неделю до встречи — принесённый на неё пропущенный дедлайн,
  не следующий год; карточка встречи и Prep читают дату так же,
  заметки без даты встречи в пути остаются на прежней догадке от
  «сегодня»; отметка пишет `[x]` прямо
  в markdown — Obsidian и приложение всегда согласны. Список идёт от
  последней встречи к ранней, и у каждой группы стоит её дата: порядок
  считается по времени встречи из имени архива, а не по mtime файла —
  ночная ревизия трогает старые папки и поднимала их наверх как «свежие».
- **Стриминг ответов архива** — первые слова через ~1с, набор
  токен за токеном с курсором; пикер моделей чата показывает живой
  список из Ollama.
- **История ответов** — прошлые вопросы свёрнуты под текущим ответом
  и переживают перезапуск (до 50 записей на диске).
- **Вопросы и брифы по архиву** — поиск v2: стемминг русской морфологии,
  IDF (редкие слова запроса весят больше), покрытие запроса, свежесть
  файла, приоритет дистиллятов графа над сырыми стенограммами,
  разнообразие выдачи (одна встреча не занимает все слоты). Слабые
  совпадения помечаются «⚠ возможно, в архиве этого нет» — ассистент
  не сочиняет по нерелевантным кускам. Источники ответа кликабельны
  (открываются в Obsidian). При поднятом brain-компаньоне (:8100)
  добавляется семантический слой bge-m3 — ответ находится даже без
  совпадения слов.

## Формат документов

Все документы — plain-markdown, читаются без рендера: списки с жирным
ключом вместо таблиц, короткие блоки, одинаковая структура каждый раз,
главное — первой строкой (BLUF). Слои: Саммари (минута) → Минутки →
Разбор → Стенограмма.


## Поручения доходят до окна задач

Минутки называют, кто что делает и к какому сроку — промпт просит чекбокс, и
модель честно выдаёт имя, задачу и срок. Просто заворачивает строку в свой
markdown и теряет по дороге `[ ]`:

    *   **- **Дмитрий** — подготовить демо. — **Срок: завтра**.**

Окно задач ищет `^\s*[-*] \[( |x|X)\] +(.+)$`, и ничего из написанного выше
задачей для него не является. Замер по рабочему графу: 89 файлов минуток,
**4 видимые задачи**. Поручения давала каждая встреча; окно стояло пустым.

Формат приводится к нужному после генерации — детерминированно, а не более
строгой просьбой к модели: она и так «соблюдает» его как умеет и срывается
снова на следующем длинном ответе. Трогается только раздел поручений и только
его оформление: имена, формулировки и сроки остаются как написаны.

Тот же граф после нормализации: **275 задач**. Уже накопленные файлы
переводятся разово через `scripts/fix_action_items.py` (по умолчанию сухой
прогон).

Первый живой день добавил два недостающих провода. Нормализация звалась
только на ручном «Протоколе», а автосохраняемый черновик — документ,
который люди и читают: автофинализации нет — шёл мимо неё. И голая
строка-заголовок («Открытые вопросы:») не закрывала раздел поручений,
так что списки после неё утекали в окно задач ложными задачами.
Исправлено: каждая запись черновика проходит ту же нормализацию, а
голая строка-заголовок закрывает раздел, только когда называет
известную секцию минуток (открытые вопросы, риски, решения…) — одного
двоеточия на конце мало: в живом документе такая же форма у переносов
строк и меток внутри пункта.

Хвосты того же дня закрыли оставшиеся дыры: третий путь записи — mcp-инструмент
«Минутки» — теперь проходит ту же нормализацию до записи (раньше шёл мимо, и
его поручения не попадали в окно задач); причёсывание срока трогает только
хвост пункта после тире — живая формулировка «обсудить срок: завтра» больше не
переписывается; после пересборки стенограммы минутки пересобираются по
финальной стенограмме, если их никто не трогал (демон — а после первой
пересборки и сама пересборка — оставляет в live.json хеш последней
автоматической записи; файл, который всё ещё с ним совпадает, — автотекст, и
он перегенерируется с восстановленными именами, прежняя версия уходит в
transcripts/.prev; 13-минутная встреча — около 13 с на местной модели, и
вызов до десяти минут ждёт конца живой встречи), встреча без черновика
получает минутки, если речи в ней достаточно, слишком короткий для модели
финал оставляет черновик, а сайдкар с хешем находится и после наката темы
на имя встречи и хранит точный посекундный штамп встречи (`stamp`: пишет
демон при стопе и накат темы), по которому пересборка озаглавленной встречи
ищет записи вместо угадывания по минуте и не подхватывает запись соседки той
же минуты, когда своей нет; имя темы, чей сайдкар уже занят чужой сиротой,
отвергают и накат темы (файл остаётся, тема идёт только в шапку), и
`rename_meeting.py` (ничего не переименовывается, код выхода 1), близнец с
собственным посекундным
штампом встречи воссоединяет пару, а сирота-сайдкар со штампом кандидата
считается уликой соседки при разрешении записей по минуте; правленные
руками минутки только
перештамповываются —
маркер черновика снимается, метки «Собеседник N» заменяются восстановленными
именами; а когда нативный захват системного звука не поднялся, приложение
говорит вслух, что встреча пишется через BlackHole, вместо молчаливого фолбэка.
Следом два укрепления: живые имена сопоставляются только нейтральным
меткам пересборки (метка владельца больше не может унаследовать чужое
имя в финальной стенограмме), а иконка в строке меню сама становится
предупреждающим символом при critical конвейера — единственное место,
которое видно всегда.
